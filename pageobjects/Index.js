const { LoginPage } = require('./loginPage');
const { Confirmations } = require('./Confirmations');
const { OptionPanel } = require('./OptionPanel');
const { SmsFriend } = require('./SmsFriend');

module.exports = {
  LoginPage,
  Confirmations,
  OptionPanel,
  SmsFriend,
};